# User Prompt Templates

Place user prompt template files here. Each file should be a plain-text or
Markdown file that can be filled with variables at runtime, e.g.
`task_template.md`, `eval_template.md`.
