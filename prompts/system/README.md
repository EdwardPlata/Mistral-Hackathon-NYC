# System Prompts

Place system prompt files here. Each file should be a plain-text or Markdown
file named to reflect its purpose, e.g. `agent_default.md`, `eval_judge.md`.

These prompts are loaded by `agents/main_agent.py` at runtime.
